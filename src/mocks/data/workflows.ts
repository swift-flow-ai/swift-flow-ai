import { Workflow } from "../../types/workspace";

export const mockWorkflows: Workflow[] = [];

export function getWorkflowsByWorkspace(): Workflow[] {
  return [];
}

export function getWorkflowById(_workflowId: string): Workflow | undefined {
  return undefined;
}
